# Student Enrollment – Windows Forms (C#/.NET)

**Автор:** Калин Мирославов Тошев • **Фак. №:** F113093
Курс: CSCB579 – Програмиране на приложения с Microsoft Visual C# .NET

> Малко Windows Forms приложение без база данни, демонстриращо основни принципи на ООП (инкапсулация, наследяване, полиморфизъм), интерфейси, делегати/събития и работа с колекции.

---

## ⚙️ Технологии

* C# / .NET Framework 4.8
* Windows Forms (WinForms)
* Колекции: `List<T>`, `SortedList<TKey,TValue>`, `Stack<T>`
* Делегати и събития (custom delegate + EventArgs)

---

## 📦 Структура на проекта

```
StudentEnrollmentApp/
├─ StudentEnrollmentApp.sln
└─ StudentEnrollmentApp/
   ├─ StudentEnrollmentApp.csproj
   ├─ Program.cs
   ├─ Domain/
   │  ├─ Interfaces.cs      # IIdentifiable, IEnrollable, делегат и EventArgs
   │  ├─ Models.cs          # Person (abstract), Student (sealed)
   │  └─ Services.cs        # EnrollmentService (колекции, събития, Undo)
   └─ UI/
      └─ MainForm.cs        # Главната форма и логика на интерфейса
```

---

## 🚀 Стартиране

* Създай WinForms проект „Windows Forms App (.NET Framework)“ и копирай файловете в съответните папки.
* В `Program.cs` стартирай `Application.Run(new UI.MainForm());`

---

## 🧭 Какво демонстрира приложението

* **ООП**

  * *Инкапсулация:* private полета + свойства с валидация (напр. `FirstName`, `LastName`).
  * *Наследяване:* `Student : Person`.
  * *Полиморфизъм:* `override Describe()` и `ToString()`.
* **Интерфейси:** `IIdentifiable`, `IEnrollable`, `IComparable<Student>`.
* **Делегати и събития:** custom делегат `EnrollmentChangedHandler`, събитие `EnrollmentChanged` в `EnrollmentService`, обработка в `MainForm`.
* **Колекции:** `List<Student>` като основно хранилище, `SortedList<string,Student>` за бързо търсене по ФН, `Stack<Action>` за **Undo**.
* **WinForms UI:** контроли (TextBox, ComboBox, DateTimePicker, ListBox, StatusStrip), обработка на събития (Click, TextChanged), базов data binding.

---

## 🖱️ Употреба (демо сценарий)

1. Стартирай приложението – зареждат се 3 примерни студента.
2. Въведи Име, Фамилия, Дата на раждане, Фак. №, Специалност → **Add**.
3. Търси по име/ФН в полето **Search** – списъкът се филтрира в реално време.
4. **Sort: Name** / **Sort: Faculty No** – пренареждане на списъка.
5. **Remove Selected** – изтриване на избрания студент.
6. **Undo** – връщане на последното действие (добавяне/изтриване).
7. Долният статус бар показва съобщения от домейн събитията.

---

## 🧩 Архитектурни бележки

* **Слой UI** (WinForms) е „тънък“: не пази домейн състояние, а използва публичния API на `EnrollmentService`.
* **Слабо свързване:** промени в домейна се съобщават чрез **събития**; UI само се абонира и обновява изгледа.
* **Валидиран вход:** свойства с проверки хвърлят изключения → в UI се показва `MessageBox`.

---

## 📚 Ключови класове

* `Person` *(abstract)* – базов тип с `Id`, `FirstName`, `LastName`, `BirthDate`, `FullName`, `Describe()`.
* `Student` *(sealed)* – наследява `Person`, добавя `FacultyNumber`, `Major`, `IsEnrolled`, имплементира `IEnrollable` и `IComparable<Student>`.
* `EnrollmentService` – управлява добавяне/триене/търсене/сортиране/undo и излъчва `EnrollmentChanged`.
* `MainForm` – изгражда UI контролите програмно, закача обработчици и реактира на домейн събитията.

---

## 🔧 Конфигурация и полезни настройки

* Показване на повече данни в списъка: използвай `DisplayRow` свойство в `Student` и задавай `lstStudents.DisplayMember = nameof(Student.DisplayRow)`.
* По-добра четимост в `ListBox`:

  ```csharp
  lstStudents.HorizontalScrollbar = true;
  lstStudents.Font = new Font(FontFamily.GenericMonospace, 10f);
  ```

---

## 🧪 Тестване (ръчно)

* Добавяне на студент с дублиран ФН → очаквано предупреждение.
* Remove → Undo → състоянието се връща коректно.
* Търсене по част от име → филтърът работи без чувствителност към регистър.

---

## 👤 Автор

Калин Мирославов Тошев • F113093
